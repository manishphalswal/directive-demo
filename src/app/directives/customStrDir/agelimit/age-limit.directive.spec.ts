import { AgeLimitDirective } from './age-limit.directive';

describe('AgeLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeLimitDirective();
    expect(directive).toBeTruthy();
  });
});
